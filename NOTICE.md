# Notice

This package is a standalone Office artifact toolkit.

Package: `office-artifact-tool@2.8.31`

Vendored runtime dependencies included for standalone operation:

- `@officer/walnut@0.1.231` — OpenXML/.NET WASM runtime assets used by the package runtime.
- `@officer/embedded-fonts@0.1.1` — embedded font loader and decoder WASM used by the package runtime.
- `skia-canvas` — native canvas implementation for Node.js, MIT license.
- `detect-libc` — Apache-2.0 license.
- `agent-base`, `debug`, `follow-redirects`, `https-proxy-agent`, `ms`, `parenthesis`, `string-split-by` — MIT license unless their package metadata says otherwise.

The top-level package license is preserved in `LICENSE.md`.
